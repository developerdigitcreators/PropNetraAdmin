/**
 * Central Theme Configuration
 * Use this file to manage global brand colors and design tokens in JS/TS contexts.
 * For CSS, the colors are driven by CSS variables in app/globals.css.
 */

export const theme = {
  colors: {
    primary: {
      DEFAULT: '#DC2626', // Primary red
      light: '#DC26261A', // 10% opacity for backgrounds (bg-primary-light)
      hover: '#B91C1C',   // Slightly darker for hover states
    },
    // Add other brand colors here as needed
  }
};
